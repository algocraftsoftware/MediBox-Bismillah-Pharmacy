{
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "buildCommand": "PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1 npx prisma migrate deploy && npx prisma generate",
  "functions": {
    "api/index.ts": { "maxDuration": 60 }
  }
}
